
<div class="contact">
 <div class="show-title"><p>Contáctanos:</p></div>
   <div class="or-spacer">
  <div class="mask"></div>
  <span><i></i></span>


<div id="map-canvas" class="col-xs-12 col-sm-12 col-md-5 col-lg-5" ></div>

</div>

<div class="form-contact">
<%= form_tag(send_mail_path) do %>

            <%= raw negative_captcha(@captcha) %>
            <div>
                <%= negative_label_tag @captcha,:name, 'Nombre' %>
                <%= negative_text_field_tag @captcha,:name,  class: '', placeholder: 'Tu nombre', required:"required"%>
            </div>
           <div >
               <%= negative_label_tag @captcha,:mail,'mail' %>
               <%= negative_text_field_tag @captcha,:mail,  class: '', placeholder: 'Tu Email' , required:"required"%>
           </div>
           <div >
               <%= negative_label_tag @captcha,:comments, 'Comentarios' %>
               <%= negative_text_area_tag @captcha,:comments,  class: '', rows: 4,  placeholder: 'Comments...', required:"required" %>
           </div>
           <%= submit_tag nil, class: "button" %>
 <% end %>
</div>

</div>
